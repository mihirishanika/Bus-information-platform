# Frontend environment (Vite)
# Copy to frontend/.env and update values

# AWS region where your Cognito pool lives
VITE_AWS_REGION=ap-south-1

# Cognito User Pool details (App client must be WITHOUT secret)
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_USER_POOL_CLIENT_ID=

# Hosted UI domain (required for Google Sign-In)
# Use the Cognito domain you set under App integration > Domain name
# Examples:
#   your-domain.auth.ap-south-1.amazoncognito.com
#   https://your-domain.auth.ap-south-1.amazoncognito.com
VITE_COGNITO_DOMAIN=

# OAuth redirect URIs (must be added to Cognito App client Allowed callback/sign-out URLs)
# For local dev:
VITE_OIDC_REDIRECT_URI=http://localhost:5173
VITE_OIDC_LOGOUT_URI=http://localhost:5173

# Optional: API Gateway base URL from SAM deployment outputs
VITE_API_BASE=